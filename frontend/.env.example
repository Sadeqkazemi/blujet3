# Same-origin dev: leave empty and use VITE_DEV_API_PROXY for the Vite → NestJS proxy target.
VITE_API_URL=
# NestJS listen address for vite.config.ts dev proxy (default http://127.0.0.1:3000).
VITE_DEV_API_PROXY=http://127.0.0.1:3000
# Show dev OTP hint (123456) in checkout login UI. Auth always hits real /auth/otp/*.
# Set to false to hide the on-screen test-code hint.
VITE_MOCK_OTP=true
VITE_SANDBOX_AUTH=true

# Error tracking — omit to disable in local dev (CLAUDE.md Observability rules)
VITE_SENTRY_DSN=
